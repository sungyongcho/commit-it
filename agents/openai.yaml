interface:
  display_name: "Commit It"
  short_description: "Verified commits, requested reviews and Git delivery"
  default_prompt: "Use $commit-it to prepare verified scoped commits or review the requested PR under the repository's rules and existing authorization."
